function SortBar() {
  return (
    <ul style={{ listStyle: "None" }}>
      Sort by: (not working yet)
      <li>
        <button>Alphabet (A-Z)</button>
      </li>
      <li>
        <button>Recently Added</button>
      </li>
      <li>
        <button>Price (lowest to highest)</button>
      </li>
    </ul>
  );
}

export default SortBar;
